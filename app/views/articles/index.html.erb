<div class='container'>
  <% @articles.each do |article| %>
    <div class='container-small'>
      <h3> <%= link_to article.title, article_show_path(article.id) %></h3>
      <p><%= article.content %></p>
    </div>
  <% end %>
</div>
<div class='container'>
  <%= link_to 'Add article', new_article_path %>
</div>
